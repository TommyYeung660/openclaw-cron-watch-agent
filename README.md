# cron-watch-agent

Monitors OpenClaw cron jobs/runs and produces a daily summary report (Traditional Chinese).

- Daily report is generated via a workspace skill: `cron-daily-report`
- Reports are saved under `reports/`
